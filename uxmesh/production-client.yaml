#
# Chart: uxmesh-server
#

image:
  repository: quay.io/zebbra/uxmesh-client
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: zebbra-kube-pull-secret

ingress:
  enabled: false
#  annotations:
#    # kubernetes.io/ingress.class: nginx
#    kubernetes.io/tls-acme: "true"
#  hosts:
#    - host: uxmesh.k8s.zebbra.ch
#      paths:
#        - /
#  tls:
#    - secretName: uxmesh-tls
#      hosts:
#        - uxmesh.k8s.zebbra.ch
service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 500m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
